# Weekly Report Notes

## 주간보고용 상세 문구

```text
SentinelOne Custom Rule [SOC][IT] AMOS, 클로드디자인 위장 캠페인_IoC_260424 탐지 건을 확인하였다. KAMAS17 서버에서 Apache Tomcat 구동용 정상 관리 스크립트인 catalina.sh 실행 중 OUTGOING 네트워크/DNS 로그가 발생했으며, 해당 로그가 룰에 등록된 IOC IP 172[.]240[.]253[.]132와 매칭되어 탐지된 것으로 확인되었다. 대상 호스트는 스미싱 대응 크롤링 서버로 확인되어 정상 업무 과정에서 발생한 오탐 가능성이 있으나, 운영 서버의 침해 여부 식별 및 보안 가시성 유지를 위해 예외처리는 적용하지 않고 지속 모니터링하는 것으로 정리하였다.
```

## 주간보고용 축약 문구

```text
SentinelOne AMOS 캠페인 IOC 기반 Custom Rule 탐지 건 확인 결과, KAMAS17 서버의 OUTGOING 네트워크/DNS 로그가 IOC IP 172[.]240[.]253[.]132와 매칭되어 탐지된 것으로 확인됨. 대상 프로세스는 Tomcat 구동용 정상 스크립트 catalina.sh이며, 스미싱 대응 크롤링 서버의 정상 업무 과정에서 발생한 오탐 가능성이 있으나 보안 가시성 유지를 위해 예외처리 없이 지속 모니터링 예정.
```

## 완료 상태 문구

```text
분석 완료. 대상 이벤트는 Custom Rule 등록 IOC와 KAMAS17 서버의 네트워크/DNS 로그가 매칭되어 발생한 탐지로 확인됨. 대상 프로세스는 정상 Tomcat 관리 스크립트로 확인되며, 크롤링 서버 업무 특성상 오탐 가능성이 있으나 운영 서버 보안 가시성 유지를 위해 예외처리 없이 모니터링 유지로 정리함.
```

## 진행 상태 문구

```text
분석 진행 중. SentinelOne Custom Rule 기반 탐지 이벤트로 확인되며, 현재 IOC 매칭 조건과 대상 서버의 업무 성격을 기준으로 오탐 여부 및 예외처리 필요성을 검토 중.
```

## 대기 상태 문구

```text
고객사/운영부서 확인 대기. 대상 이벤트는 Custom Rule IOC 매칭으로 발생한 것으로 확인되었으며, 운영 서버 보안 가시성 유지를 고려해 예외처리 적용 여부 회신 대기 중.
```

## SentinelOne 노트용 문구

```text
Custom Rule [SOC][IT] AMOS, 클로드디자인 위장 캠페인_IoC_260424에 등록된 IOC IP 172[.]240[.]253[.]132와 KAMAS17 서버의 OUTGOING 네트워크/DNS 로그가 매칭되어 탐지된 건으로 확인됨. 대상 프로세스 catalina.sh는 Apache Tomcat 구동용 정상 관리 스크립트이며, 해당 호스트는 스미싱 대응 크롤링 서버로 확인되어 정상 업무 과정에서 발생한 오탐 가능성이 있음. 다만 침해 여부 식별 및 보안 가시성 유지를 위해 예외처리는 미적용하고 지속 모니터링하는 것으로 전달받음.
```

## 자소서/면접용 개념 문장

```text
SentinelOne Custom Rule 기반 탐지 이벤트와 Endpoint Alert 간 매핑 구조를 분석하였다. IOC 기반 Custom Alert가 Treat as threat 설정에 따라 Endpoint Alert로 연계될 수 있음을 확인하고, Custom alerts 탭의 룰 매칭 관점과 Endpoint 탭의 자산·프로세스·Storyline 관점 차이를 정리하였다. 또한 정상 업무 서버에서 발생한 IOC 매칭 이벤트에 대해 오탐 가능성과 보안 가시성 유지 필요성을 함께 검토하여 예외처리 여부 판단 근거를 정리하였다.
```

